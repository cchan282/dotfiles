#!/bin/bash

# Get CPU usage without extra spaces
cpu_usage=$(top -bn1 | grep "Cpu(s)" | sed "s/.*, *\([0-9.]*\)%* id.*/\1/" | awk '{print 100 - $1}')

# Format to 2 decimal places and remove trailing spaces
printf "%.2f%%" "$cpu_usage"
