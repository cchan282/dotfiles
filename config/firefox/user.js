// Firefox user.js configuration
// This file contains custom Firefox preferences

// Disable Alt key menu activation to prevent interference with window manager mod keys
// This prevents Alt and Alt+Tab from accidentally triggering Firefox's menu bar
user_pref("ui.key.menuAccessKeyFocuses", false);
